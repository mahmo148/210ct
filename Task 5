##WRite the pseudocode corresponding to functions for addition, subtraction and
##multiplication of two matricies. Computer A = B*C-2*(B+C)



#FUNCTION addition(matrix1, matrix2):
#	For row in 0 to order(N)
#		For column in 0 to order(N)
#OUTPUT(row, column) --> matrix1(row, column) + matrix2(row, column)

#FUNCTION subtraction(matrix1, matrix2):
#	For row in 0 to order(N)
#		For column in 0 to order(N)
#OUPUT(row, column) --> matrix1(row, column) - matrix2(row, column)

#FUNCTION multiplication(matrix1, matrix2):
#	For row in 0 to order(N)
#		For column in 0 to order(N)
#OUTPUT(row, column) --> matrix1(row) * matrix2(column)
	
#x = multiplication(B, C)	
#y = addition(B, C)
#z = addition(y, y)
#A = subtraction(x, z)
#OUTPUT = A

##Big O Notation = O(3N^2)
